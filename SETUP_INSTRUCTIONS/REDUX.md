## ADD REDUX
> In Terminal:
  cd into <react app folder> ("client")
  npm install redux react-redux

## ADD REDUX TOOLKIT
> In Terminal:
  cd into <react app folder> ("client")
  npm install @reduxjs/toolkit